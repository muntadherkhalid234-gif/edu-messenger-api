[project]
name = "edu-messenger-api"
version = "1.0.0"
description = "Backend API for Arabic Educational Messaging App"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "sqlalchemy>=2.0.0",
    "pydantic>=2.0.0",
    "aiofiles>=24.1.0",
]

[tool.uvicorn]
app = "main:app"
